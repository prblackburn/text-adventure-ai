# CLAUDE.md — AI Assistant Guide for text-adventure-ai

This file provides context for AI assistants working on this repository.

---

## Project Overview

**Ashveil** is an AI-powered text adventure game running on Cloudflare Workers. Players navigate procedurally generated stories across three themes (Noir Detective, Fantasy Dungeon, Sci-Fi Colony) structured as a 5-beat narrative arc, with responses generated by Groq's LLaMA model.

## Tech Stack

| Layer | Technology |
|---|---|
| Runtime | Cloudflare Workers (via Wrangler 4) |
| Frontend | React 19 + React Router 7 (SSR) |
| Styling | Vanilla Extract 1.20 (CSS-in-JS, zero runtime) |
| Database | Cloudflare D1 (SQLite) |
| Cache | Cloudflare KV |
| LLM | Groq API — `llama-3.3-70b-versatile` |
| Build | Vite 6 |
| Language | TypeScript (strict mode, ES2022) |
| Package manager | pnpm |

## Directory Structure

```
text-adventure-ai/
├── app/
│   ├── components/          # Reusable UI components (paired .tsx + .css.ts)
│   │   ├── BeatProgress.tsx # 5-beat story progress bar
│   │   ├── GameLog.tsx      # Scrollable game transcript
│   │   ├── InputBar.tsx     # Player command input form
│   │   └── DevOverlay.tsx   # Dev-mode debug panel (toggle with D key)
│   ├── game/                # Core game logic (no React)
│   │   ├── types.ts         # Shared TypeScript interfaces
│   │   ├── beats.ts         # 5-beat story structure definition
│   │   ├── worldSeed.ts     # Random theme generation (3 seeds)
│   │   ├── worldRules.ts    # Per-theme scene rules (items, NPCs, exits)
│   │   ├── classifier.ts    # Keyword-based intent classification
│   │   └── promptBuilder.ts # LLM system/user prompt construction
│   ├── lib/                 # Infrastructure clients
│   │   ├── db.ts            # D1 SQLite CRUD (sessions, turns)
│   │   ├── responseCache.ts # KV cache read/write for generic responses
│   │   └── stream.ts        # Groq streaming API client (SSE)
│   ├── routes/
│   │   ├── _index.tsx           # Home page — "Begin Adventure" button
│   │   ├── api.session.ts       # POST /api/session — create session, redirect to /play
│   │   ├── api.action.ts        # POST /api/action — main game loop
│   │   └── play.$sessionId.tsx  # Game play page (SSR loader)
│   ├── styles/
│   │   └── global.css.ts    # Body reset and global styles
│   ├── root.tsx             # Root React Router layout
│   ├── entry.server.tsx     # Server-side rendering entry
│   └── types.d.ts           # Cloudflare env bindings (D1, KV, secrets)
├── worker/
│   └── index.ts             # Cloudflare Workers entry — dispatches to React Router
├── migrations/
│   └── 0001_initial.sql     # D1 schema: sessions, turns, response_pool
├── wrangler.toml            # Cloudflare config (D1, KV, observability)
├── vite.config.ts           # Build: cloudflare-devproxy, vanilla-extract, react-router
├── react-router.config.ts   # SSR enabled
├── tsconfig.json            # Strict TS, ES2022, react-jsx
└── .prettierrc              # 140-char lines, tabs, single quotes, semicolons
```

## Development Workflow

### Local Development
```bash
pnpm install          # Install dependencies
pnpm dev              # Start dev server (React Router dev mode)
pnpm start            # Start via Wrangler (closer to production)
```

### Build & Deploy
```bash
pnpm build            # Vite production build
pnpm deploy           # build + wrangler deploy
```

### Database Migrations
```bash
wrangler d1 migrations apply text-adventure-ai-db --local   # local dev
wrangler d1 migrations apply text-adventure-ai-db           # production
```

### Environment Variables
- `GROQ_API_KEY` — injected as a Cloudflare Workers secret, never committed
- Accessed as `env.GROQ_API_KEY` inside route actions

### Dev Mode
Append `?dev` to any `/play/{sessionId}` URL to enable the `DevOverlay` debug panel. Press `D` to toggle it while not focused on the input field.

## Architecture & Request Flow

```
Browser
  └─ POST /api/session → create session in D1 → redirect to /play/{id}
  └─ GET /play/{id}    → SSR loader fetches session/turns/rules → renders game page
  └─ POST /api/action  → main game loop:
       1. Parse sessionId, player input
       2. Classify intent (keyword matching — no LLM)
       3. Entity validation — if target doesn't exist in scene, return early (no LLM call)
       4. Cache lookup — for examine/explore, check KV first
       5. Build system prompt (beat, theme, world rules, scene context)
       6. Call Groq API (streaming, max 150 tokens)
       7. Store response in D1 (turns table) + optionally KV cache
       8. Advance beat if turn threshold met (every TURNS_PER_BEAT = 3 turns)
       9. Redirect to /play/{id} (PRG pattern)
```

## Key Concepts

### Story Structure (Beats)
Five beats defined in `app/game/beats.ts`:
- **0 — Introduction:** Player discovers their situation
- **1 — Rising Action:** Complications emerge
- **2 — Confrontation:** Central conflict intensifies
- **3 — Climax:** Peak tension moment
- **4 — Resolution:** Story concludes

Beat advances every `TURNS_PER_BEAT = 3` substantive turns in `api.action.ts`.

### World Themes
Three themes in `app/game/worldSeed.ts` and `app/game/worldRules.ts`:
- **Noir Detective** — 1940s mystery, 5 scenes, 4 characters
- **Fantasy Dungeon** — Underground labyrinth, 5 scenes, magical constraints
- **Sci-Fi Colony** — Remote planet, 5 scenes, technology and isolation

Each theme has per-scene definitions: `items[]`, `characters[]`, `exits[]`, `constraints[]`, and a `globalRules[]` array.

### Intent Classification
`app/game/classifier.ts` uses keyword matching (not LLM) to classify player input into 7 types:
`explore`, `interact`, `combat`, `dialogue`, `examine`, `use`, `other`

Only `examine` and `explore` intents are eligible for KV caching.

### Caching Strategy
- **KV cache key:** `{intent}:{subject}` (e.g., `examine:door`)
- Cache is consulted before calling Groq; hits skip the LLM entirely
- Cache writes also go into D1's `response_pool` table for analytics

## Code Conventions

### Naming
- `camelCase` — variables, functions
- `PascalCase` — React components, TypeScript interfaces/types
- `UPPER_SNAKE_CASE` — module-level constants (`BEATS`, `SEEDS`, `TURNS_PER_BEAT`)
- File names: `lowercase.tsx` / `lowercase.ts`
- CSS files: `ComponentName.css.ts` paired with `ComponentName.tsx`

### TypeScript
- **Strict mode** is enabled — no implicit `any`, no loose nullability
- All shared types live in `app/game/types.ts`
- Cloudflare binding types declared in `app/types.d.ts`
- Use `interface` for object shapes, `type` for unions/aliases

### React Patterns
- Functional components only — no class components
- React Router 7 conventions: use `loader` for data fetching, `action` for mutations
- No client-side data fetching — all data comes from the SSR loader
- Auto-scroll and keyboard handlers use `useRef` + `useEffect`

### Styling (Vanilla Extract)
- All styles are in `*.css.ts` files — **no inline styles**, no Tailwind, no plain CSS files
- Use `style()` for component classes, `globalStyle()` for global rules
- Color tokens are used directly (not via CSS variables) — primary gold: `#f5c518`, background: `#0a0a06`
- Font: `"Courier New", Courier, monospace` throughout

### Formatting
Enforced by `.prettierrc`:
- **Tabs** (not spaces)
- **Single quotes**
- **Semicolons**
- **140 character** line width
- Run `pnpm prettier --write .` before committing if needed

### Error Handling
- Route actions return `Response` objects with appropriate status codes
- Minimal logging — errors surface via Cloudflare observability (enabled in `wrangler.toml`)
- No custom error boundaries yet

## Database Schema

**sessions** — one row per game
```sql
id TEXT PRIMARY KEY, world_seed TEXT, current_beat INTEGER, created_at INTEGER, updated_at INTEGER
```

**turns** — one row per player action
```sql
id INTEGER PK, session_id TEXT (FK), player_input TEXT, ai_response TEXT, intent TEXT, beat INTEGER, created_at INTEGER
```

**response_pool** — cached LLM responses for analytics/reuse
```sql
id INTEGER PK, beat INTEGER, response_type TEXT, content TEXT, created_at INTEGER
-- Index on (beat, response_type)
```

## What's Done vs. Planned

### Completed
- Cloudflare Workers + React Router 7 SSR scaffold
- D1 session/turn persistence
- Groq LLM integration with streaming
- 3-theme world system with full scene rules
- 5-beat narrative progression
- Keyword-based intent classifier
- Entity pre-validation (no LLM call for invalid targets)
- KV response caching for examine/explore intents
- Retro terminal UI (gold-on-black, monospace)
- Developer overlay for game state inspection
- Vanilla Extract styling migration

### Planned (from README roadmap)
- Inventory system
- NPC relationship tracking
- Branching story endings
- Combat mechanics
- Rate limiting
- Multiplayer / session sharing
- Test coverage (no tests currently exist)

## Testing

**No tests exist yet.** There is no test runner configured. When adding tests, use **Vitest** (compatible with Vite) and place test files alongside source with `.test.ts` / `.test.tsx` extensions.

## Common Pitfalls

1. **No Node.js APIs by default** — `nodejs_compat` flag is set in `wrangler.toml`, but prefer Web APIs where possible.
2. **D1 is SQLite, not Postgres** — no JSON columns, limited `ALTER TABLE` support. Add new columns via migration files in `migrations/`.
3. **KV is eventually consistent** — don't rely on KV reads immediately after writes in the same request.
4. **Vanilla Extract requires Vite plugin** — don't import `.css.ts` files from non-Vite contexts.
5. **React Router 7 uses file-based routing** — route filenames define URL segments (`play.$sessionId.tsx` → `/play/:sessionId`).
6. **Groq streaming is SSE** — `app/lib/stream.ts` manually parses `data:` lines; handle `[DONE]` sentinel.
7. **Beat index vs. beat count** — beats are 0-indexed (0–4). `current_beat` in D1 is the index.
